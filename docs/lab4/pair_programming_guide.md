# Шпаргалка для защиты ЛР №4 (сценка)

Чтобы "Внук" (препод) осталась довольна и не сказала "это вам иишка написала", нужно показать **живой диалог** и **совместное решение проблемы**.

### 1. Подготовка (что должно быть в коде)
Заранее добавьте в `MinesveeperEngine.kt` перечисление событий:
```kotlin
sealed class GameEvent {
    object Explosion : GameEvent()
    object Win : GameEvent()
    data class FlagSet(val type: Int) : GameEvent()
}
```
И добавьте переменную в класс `MinesveeperEngine`:
`var onEvent: ((GameEvent) -> Unit)? = null`

### 2. Сценарий "Сценки" (на 3-5 минут)

**Роли:**
*   **Водитель (Иван):** Сидит за клавиатурой.
*   **Штурман (Алексей):** Стоит рядом, смотрит в монитор.

**Диалог:**
1.  **Штурман:** "Слушай, у нас сейчас при взрыве просто меняется `isGameOver`. Давай добавим вызов события, чтобы в UI мы могли запустить вибрацию. Пиши в методе `revealCell` перед `return`."
2.  **Водитель:** Начинает писать `onEvent?.invoke(GameEvent.Explosion)`.
3.  **Штурман:** "Стой! Если это анти-мина (тип -1), может сделаем другой звук? Добавь проверку `if (cell.mineValue == -1)`."
4.  **Водитель:** "Точно. (Пишет код). А как мы назовем событие для анти-мины? `AntiExplosion`?"
5.  **Штурман:** "Давай просто добавим параметр в `Explosion(val isAnti: Boolean)`. Так будет чище архитектурно."
6.  **Водитель:** Исправляет `sealed class` и вызов. "О, смотри, Android Studio подсвечивает ошибку в `MainActivity`, потому что там теперь нужно обрабатывать новый параметр."
7.  **Штурман:** "Вот видишь, парно прогать быстрее — я бы этот баг в UI только через полчаса заметил при запуске."

### 3. Советы для "красивого отчета" на GitHub
*   **Сделайте фото:** Реально сядьте вдвоем за один ноут, пусть кто-то третий вас сфоткает. Вставьте это фото в `report.md`. Это 100% доказательство, что вы не просто чат-бот мучали.
*   **Git Log:** В отчете можно упомянуть: "Мы специально делали коммиты от имени обоих авторов (Co-authored-by)".
*   **Меньше воды:** Внук не любит "мишуру". В `report.md`, который я создал, текст по делу, без лишних вступлений.

**Удачи на защите! Если препод начнет валить по коду — зови, разберемся.**
